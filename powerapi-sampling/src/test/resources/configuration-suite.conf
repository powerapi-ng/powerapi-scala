powerapi.sampling.interval = 250ms

powerapi.sampling.nb-samples = 3

powerapi.sampling.dvfs = true

powerapi.sampling.turbo = true

powerapi.sampling.steps = [25, 75, 100]

powerapi.sampling.step-duration = 3

powerapi.sampling.cpu-base-frequency = 0.133

powerapi.sampling.cpu-max-frequency = 2.66

powerapi.cpu.topology = [
  { core = 0, indexes = [0, 4] }
  { core = 1, indexes = [1, 5] }
  { core = 2, indexes = [2, 6] }
  { core = 3, indexes = [3, 7] }
]

powerapi.libpfm.configuration = [ 0, 1, 2, 10 ]

powerapi.sampling.unhalted-cycles-event = "THREAD_P"

powerapi.sampling.ref-cycles-event = "REF_P"

powerapi.sampling.sampling-directory = "test-samples"

powerapi.sampling.processing-directory = "test-processing"

powerapi.sampling.computing-directory = "test-computing"
